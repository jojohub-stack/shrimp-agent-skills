【2026-03-29 CLI Proxy/密鑰/專案備份協調日誌】
- 確認 shrimp-agent-memory 專案過去整合的 CLI Proxy API 主程式、scripts/cli-proxy-api.py 遺失，disk 目錄及 find 未發現備份。
- Rene 因前任維護失誤，git 未及時 push，repo/壓縮包均無存檔，scripts/ 及 .py 檔案均疑似被誤刪。
- 指導可用最小 Python(NODE) 或 bash/sh 快速復刻兼容 proxy-api 指令，待用戶決定語言切入點後復原。
- secrets.env（金鑰檔）及 credentials/ 下非 git 資源也全部遺失，現已無 Gemini/OpenAI 等金鑰需全部新建與備份。
- 提供標準密鑰整合分層設計（主 secrets.env，各 project .env，自動同步腳本，權限建議與 git-crypt 等備援機制）。
- 建議後續所有敏感檔案 600 權限，並可規劃自動備份（同步雲端/加密或私有倉庫）。
- 待用戶列出現有密鑰與想整合方式，可直接給全套 config/腳本模板與範例。
